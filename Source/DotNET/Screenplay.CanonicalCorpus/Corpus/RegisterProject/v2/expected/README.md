# RegisterProject v2 expected vectors

`esm-v4.json` is the current ESM vector for the evolved scenario. `esm-v2.json` is a retired pre-lineage snapshot, retained only for historical comparison; it is not an executable expectation and must not be used by consumers.
